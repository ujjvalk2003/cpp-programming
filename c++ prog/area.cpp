#include <iostream>
#include <cmath>
using namespace std;


int main()
{
    float radius,area;
    cout<<"enter the radius";
    cin>>radius;
    area=3.14*pow(radius,2);
    cout<<"the area of the circle is "<<area;
    return 0;
}